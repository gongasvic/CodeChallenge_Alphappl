<% if @order.errors.any? || @order_item.errors.any? %>
	alert("Erro a processar o pedido | mal implementado | não descobri o erro a tempo de acabar a interface")
<% else %>
  	$(".cart-text").html("<%= escape_javascript(render 'layouts/cart_text') %>")
<% end %>